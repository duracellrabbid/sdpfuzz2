name: CI

on:
  push:
  pull_request:

jobs:
  unit:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.11"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -e .[dev]

      - name: Dependency scan (high/critical)
        run: python scripts/check_dependency_vulns.py

      - name: Ruff
        run: ruff check .

      - name: Mypy
        run: mypy .

      - name: Pytest (exclude integration)
        run: pytest -m "not integration" --cov=src/sdpfuzz2 --cov-report=term-missing --cov-fail-under=90

  integration-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -e .[dev]

      - name: Integration placeholder
        run: pytest -m integration --no-cov
